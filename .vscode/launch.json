{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug WeatherApp",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/weather_app.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true,
      "MIMode": "gdb",
      "setupCommands": [
        { "description": "Enable pretty-printing for gdb", "text": "-enable-pretty-printing", "ignoreFailures": true }
      ],
      "preLaunchTask": "Build WeatherApp"
    }
  ]
}
